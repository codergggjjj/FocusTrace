# FocusTrace v1.0.5

- 暂停专注时显示暂停过渡弹窗，实时显示已暂停时长，并提供“继续专注”按钮。
- 页面重建后仍可恢复暂停时长，暂停时间不会计入有效专注时长。
- 单次专注严格超过 5 分钟才计入统计；5 分钟及更短记录仍保留在历史中并标注为未计入统计。
- 该规则同步应用于日、周、月统计、趋势图、热力图和待办累计时长。

Android 8.0 及以上。APK 启用 R8 优化与资源收缩，使用原有 Android Debug 证书签名，可覆盖安装同签名旧版本，无需卸载。

验证：Debug 编译通过；统计专项测试 5 项通过；暂停、页面重建、继续专注专项测试通过。
